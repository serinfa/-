# 햄스터봇 AI 로봇축구 프로그램 사용 매뉴얼

## 1. 전체 화면 구성

프로그램 상단에 4개의 제어 패널이 있고, 그 아래 실시간 카메라 영상이 표시됩니다.

| 패널 | 위치 | 주요 기능 |
|---|---|---|
| 경기 제어 | 맨 왼쪽 | 시작/정지, 경기 시간, 점수 초기화 |
| 로봇 설정 | 두 번째 | 로봇 선택, 속도/각도 조절, 공 색상 보정, 골대 지정 |
| 타이머/점수판 | 중앙(주황색) | 남은 시간, AI : Player 점수 |
| 연결/인식 상태 | 맨 오른쪽 | 카메라·공·로봇 인식 상태, 하드웨어 점검, 매핑 보정 |

---

## 2. 경기 제어

- **시작 / 정지**: 클릭하면 경기가 시작되고 버튼이 빨간색 "정지"로 바뀝니다. 다시 누르면 정지하고 모든 로봇이 멈춥니다.
- **시간 설정**: 경기 시간을 1~5분 사이로 설정합니다. (경기 중에는 변경 불가)
- **점수 초기화**: AI/Player 점수를 0:0으로 되돌리고, 득점 잠금 상태도 초기화합니다.

---

## 3. 로봇 설정

### 3.1 동작 로봇 선택
- **1번(ID x)만** / **2번(ID x)만**: 선택한 로봇 한 대만 공을 추적합니다.
- **두 대 모두**: 공에 더 가까운 로봇 한 대만 자동으로 공을 쫓고, 나머지 한 대는 정지합니다. (두 로봇이 동시에 움직이며 서로 얽혀 뱅뱅 도는 문제를 막기 위한 설계)

> 괄호 안의 ID 번호는 그 로봇에 실제로 붙은 아루코 마커 ID이며, "자동 매칭"이나 "매핑 뒤집기"를 쓰면 이 표시도 자동으로 갱신됩니다.

### 3.2 속도 조절
로봇의 최대 직진 속도를 10~100 사이로 설정합니다. (기본값 42)

### 3.3 각도 조절
공이 로봇(마커) 정면 기준 좌우 몇 도 이내로 들어오면 조향 보정 없이 곧바로 직진할지를 5~90도 사이로 설정합니다. (기본값 ±30도)
- 값을 **낮추면** 더 정밀하게 정렬한 뒤에만 직진 (방향은 정확하지만 반응이 느려질 수 있음)
- 값을 **높이면** 좀 덜 정렬돼도 빨리 직진 (반응은 빠르지만 공을 살짝 빗맞힐 수 있음)
- 로봇이 각도 추정 오차로 계속 제자리에서 같은 방향으로 도는 증상이 있을 때, 이 값을 높이면 더 빨리 회전 루프에서 빠져나올 수 있습니다.

### 3.4 공 색상 보정 (영상 클릭)
1. 버튼 클릭 → "공을 클릭하세요..." 상태로 전환
2. 아래 영상에서 실제 공 위치를 클릭
3. 클릭 지점 주변 색상을 자동 분석해 새로운 HSV 인식 범위로 갱신 (조명이 바뀌었을 때 유용)

### 3.5 골대 지정 (클릭=자동인식 / 드래그=직접지정)
- **AI골대** / **P골대** 버튼 클릭 → 버튼이 "클릭/드래그..." 상태로 전환 → 영상에서 동작:
  - **클릭만 하면**: 클릭 지점의 색상과 연결된 영역을 자동으로 찾아 그 경계를 골대 영역으로 지정합니다. (색상이 균일하지 않으면 "색상 영역을 찾지 못했습니다" 경고가 뜨며, 이때는 드래그로 지정하세요.)
  - **드래그하면**: 원하는 범위를 직접 사각형으로 지정합니다.
- **초기화**: 지정한 골대 위치를 지우고 다시 색상(빨강=AI골대, 파랑=Player골대) 자동 인식으로 되돌립니다.

---

## 4. 타이머 / 점수판

- 남은 시간과 AI : Player 점수가 표시됩니다.
- 공이 골대 영역에 들어오면 자동으로 득점 처리되고, 2초간 로봇이 멈추며 "AI GOAL!" / "PLAYER GOAL!" 문구가 화면에 잠깐 표시됩니다.
- 창 크기를 줄이면 점수/시간 글자 크기가 자동으로 축소되어 잘리지 않습니다.

---

## 5. 연결 / 인식 상태

### 5.1 상태등
| 항목 | 초록(정상) | 빨강(비정상) |
|---|---|---|
| 카메라 | 연결됨 | 연결 끊김 |
| 축구공(노랑) | 인식됨 | 미인식 |
| 로봇 1 / 로봇 2 | 인식됨 | 미인식 |

### 5.2 카메라 재연결
카메라 스트림이 끊겼을 때 재연결을 시도합니다.

### 5.3 연결/하드웨어 점검
h1, h2 로봇에 순서대로 부저를 울려 통신이 정상인지 확인하고, 현재 공/로봇 인식 상태를 요약해서 보여줍니다.

### 5.4 R1 / R2 부저
화면에 "R1"/"R2"로 표시되는 로봇이 실제로 어느 물리 로봇인지 확인하는 버튼입니다. R1 버튼을 누르면 R1에 매핑된 로봇만 삑 소리를 냅니다. 화면 라벨과 실제 소리 나는 로봇이 다르면 매핑이 꼬인 것입니다.

### 5.5 자동 매칭 / 매핑 뒤집기
로봇의 BLE 연결 순서는 세션마다 바뀔 수 있어, R1/R2와 실제 로봇의 대응 관계가 매번 달라질 수 있습니다. 이를 바로잡는 버튼입니다.

- **자동 매칭** (우선 사용): 클릭하면 현재 두 마커의 위치를 기록 → h1 로봇을 짧게(약 0.5초) 제자리 회전 → 다시 위치를 비교해서, 실제로 움직인 마커를 h1(R1)로, 나머지를 h2(R2)로 자동 매핑합니다. 두 마커의 움직임 차이가 뚜렷하지 않으면 안전하게 적용을 보류하고 경고를 띄웁니다.
  - 사용 전 조건: 로봇 두 대가 모두 연결되어 있고, 카메라에 두 마커가 모두 잘 보여야 합니다.
- **매핑 뒤집기**: 자동 매칭이 애매하거나 실패했을 때, 코드를 고치지 않고 클릭 한 번으로 R1/R2 매핑을 즉시 반대로 바꿉니다.
- 매핑이 바뀌면 라디오 버튼, 상태 라벨, 부저 버튼의 ID 표시가 모두 자동으로 갱신됩니다.

**권장 순서**: 로봇 전원을 켜고 카메라에 둘 다 보이게 한 뒤 → "자동 매칭" 클릭 → 결과가 의심스러우면 R1/R2 부저로 재확인 → 필요하면 "매핑 뒤집기".

### 5.6 디버그 정보 표시
체크하면 영상에 다음 정보가 추가로 표시됩니다.
- 인식된 모든 아루코 마커 ID 목록
- 각 로봇의 마커 ID, 주행 모드(PUSH/STRAIGHT/TURN/DRIVE), 각도 오차(err), 좌/우 바퀴 명령값(L/R)

---

## 6. 영상 화면에서 직접 하는 조작

| 상태 | 클릭 | 드래그 |
|---|---|---|
| 공 색상 보정 모드 | 공 위치 클릭 → 색상 보정 | - |
| 골대 지정 모드 | 골대 안쪽 클릭 → 자동 인식 | 원하는 범위 직접 지정 |

---

## 7. 자율 주행 로직 요약 (참고용)

공과의 거리·각도에 따라 아래 순서로 동작이 결정됩니다.
1. **PUSH** – 공에 거의 닿았고(접촉 거리 이내) 방향도 맞으면(오차 40° 미만) 그냥 직진
2. **STRAIGHT** – 공이 마커 정면 기준 ±각도(설정값, 기본 30°) 이내면 조향 보정 없이 곧바로 직진
3. **TURN** – 오차가 75°를 넘으면 제자리 회전 (한 번 정한 회전 방향을 유지해 좌우 흔들림 방지)
4. **DRIVE** – 그 사이 각도(각도 설정값~75°)에서는 전진하면서 비례 조향으로 미세 조정

---

## 8. 문제 해결 가이드

| 증상 | 확인할 것 |
|---|---|
| 로봇이 공 반대쪽으로 계속 같은 방향으로 회전 | "각도 조절" 값을 높여보기, 디버그 표시로 err 값 변화 확인 |
| 공이 앞에 있는데 다른 로봇이 움직임 | "R1/R2 부저"로 매핑 확인 → 틀리면 "자동 매칭" 또는 "매핑 뒤집기" |
| 바닥을 공으로 오인식 | "공 색상 보정" 재실행, 공을 가리고도 "인식됨" 뜨는지 확인 |
| 골대가 잘 안 잡힘 | 클릭 자동인식 대신 드래그로 직접 지정 |
| 두 대 모드인데 한 대만 움직임 | 의도된 동작 (공에 가장 가까운 1대만 추적) |
